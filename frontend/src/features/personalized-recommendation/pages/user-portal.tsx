/**
 * @deprecated Use TaxWise routes (`/taxwise/recommendations`, etc.) instead.
 * Kept as a thin re-export for any stale imports.
 */
export { PortalSummaryRedirect as UserPortalPage } from "@/pages/user-view/portal-summary-redirect";
