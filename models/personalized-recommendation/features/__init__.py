"""Feature engineering for ranker inputs (Phase 2).

Computes disposable income, savings rate, debt-to-income, age, and boolean
eligibility flags. Also hosts the categorical encoders / scalers pickled into
``artifacts/`` so inference reproduces training exactly.
"""
