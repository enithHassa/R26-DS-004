{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://r26-ds-004.local/evaluation/frozen/nlu_parse_request.schema.json",
  "title": "NLUParseRequest",
  "description": "M5-frozen request body for POST /api/v1/nlu/parse (Phase 2). Update this file when the Pydantic model changes and bump gate_id in phase2_M5_baseline.json.",
  "type": "object",
  "additionalProperties": false,
  "required": ["utterance"],
  "properties": {
    "utterance": {
      "type": "string",
      "minLength": 1
    },
    "top_k": {
      "type": ["integer", "null"],
      "minimum": 1,
      "maximum": 50
    },
    "intent_hint": {
      "type": ["string", "null"],
      "description": "Optional upstream routing label; echoed as response.intent."
    }
  }
}
